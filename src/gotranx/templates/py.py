from textwrap import dedent

INIT_STATE_VALUES = dedent(
    """
def init_state_values({args}):
    {code}
""",
)

INIT_STATE_VALUES = dedent(
    """
def init_state_values({args}):
    {code}
""",
)
